using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Ns24
{
    public class No24 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("No24");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
